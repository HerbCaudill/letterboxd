import { generatePuzzle } from './generatePuzzle'

describe('generatePuzzle', () => {
  it('should work', () => {
    // expect(generatePuzzle('FREAKIEST THONGS')).toEqual([
    //   new Set(['F', 'R', 'E']),
    //   new Set(['A', 'K', 'I']),
    //   new Set(['S', 'T', 'H']),
    //   new Set(['N', 'G', 'O']),
    // ])
  })
})
